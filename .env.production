# The static site is hosted on Vercel, but the guestbook backend (Cloudflare
# Pages Function + D1) lives on Cloudflare. Point the frontend at the Cloudflare
# deployment so the guestbook works cross-origin (the API sends CORS: *).
VITE_GUESTBOOK_API=https://namkhatashioedzer.pages.dev
